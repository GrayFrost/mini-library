
      export default function() {
        function update() {}
        let count = 0;
const updateCount = () => {
    count++;
};

        var lifecircle = {
          create(target) {
            
          },
          update(changed) {
            
          },
          destroy(target) {
            
          }
        };
        return lifecircle;
      }
    